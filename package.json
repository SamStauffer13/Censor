{
  "name": "censor",
  "version": "3.0.0",
  "scripts": {
    "start": "npm run build | live-server --entry-file=integration-tests.html --wait=500",
    "lint": "eslint src/",
    "build": "watchify -d -v -t babelify src/content-scripts/content-script-init.js -p [minifyify --no-map] -o app/content-app.js -u src/content-scripts/content-script-specs.js"
  },
  "dependencies": {},
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-core": "^6.24.1",
    "babel-preset-es2015": "^6.16.0",
    "babelify": "^7.3.0",
    "browserify": "^14.3.0",
    "eslint": "^3.8.1",
    "jasmine": "^2.6.0",
    "opener": "*",
    "watchify": "^3.9.0"
  },
  "babel": {
    "presets": [
      "es2015"
    ]
  },
  "eslintConfig": {
    "env": {
      "browser": true,
      "es6": true,
      "jasmine": true
    },
    "extends": "eslint:recommended",
    "rules": {
      "brace-style": "off",
      "no-console": "error",
      "no-unused-vars": "error",
      "no-undef": "error",
      "no-extra-parens": "error",
      "no-unreachable": "error",
      "no-multi-spaces": "error",
      "yoda": "error"
    }
  }
}
