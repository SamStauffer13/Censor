{
  "name": "censor",
  "version": "3.0.0",
  "scripts": {
    "start" : "npm run build && opener spec-runner.html && livereload src",
    "build": "babel src --watch --out-dir lib --ignore specs.js"
  },
  "dependencies" :{

  },
  "devDependencies": {
    "babel-cli": "^6.16.0",
    "babel-preset-es2015": "^6.16.0",
    "opener": "*",
    "livereload" : "*"
  },
    "babel": {
      "presets": ["es2015"]
  }
}